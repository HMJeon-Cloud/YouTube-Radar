# 겨울잠 아웃라이어 파인더 — v4.0 「워치리스트 판」 · 2026-09-10

**형태** 단일 `index.html` · HTML + CSS + Vanilla JS · 빌드 없음 · GitHub Pages
**전 버전** v3.2 (2026-09-10)
**성격** 기능 추가가 아니라 **틀 교체**다. v3.2 실측에서 드러난 네 가지는 점수 공식이나 필터로 고쳐지지 않는 구조 문제였다.

---

## 1. v3.2에서 무엇이 막혔나

2026-09-09 실행 실측: 후보 58,619건 보존 · 표시 3,604건 · 상위 100건 전부 ≤3분 쇼츠 · 우선도 전부 100/100 · 등록 채널 스캔 404/3,353에서 쿼터 소진.

| # | 증상 | 진짜 원인 | v3.2의 대응 | 왜 부족했나 |
|---|---|---|---|---|
| 1 | 3,353채널 중 404채널에서 쿼터 소진 | 유니버스 크기가 하루 쿼터를 구조적으로 초과 (전체 6,706유닛 필요) | 실행 전 가능 채널 수 계산 → 확인창 | 계산해서 알려줄 뿐 한 바퀴는 여전히 9일. **어제 본 채널과 오늘 본 채널이 섞이면 순위가 스캔 순서에 좌우된다** |
| 2 | 상위 100건이 전부 짤·예능 재편집 | 그런 채널이 유니버스에 들어와 있고, 하루 10편씩 올린다 | 형태 기본 `long` · 채널당 상한 | 영상 단위 필터로는 못 막는다. 한 채널이 양산하면 어떤 상한을 걸어도 결과를 잠식한다 |
| 3 | 우선도 100/100 포화 | `min(1, log10(1+값)/log10(1+상한))` — 상한 초과분은 전부 만점 | 상한을 500만·500배·100배로 상향 | 상한을 올리면 포화 지점이 미뤄질 뿐이다. **순위 정보가 사라진다는 성질 자체는 그대로** |
| 4 | 앱 로딩 수십 초 | `restoreTabs`가 IndexedDB 5.8만 건 전량 로드 | `KEEP_MAX=12,000`으로 잘라내기 | 1.2만 건도 전량 로드다. 그리고 **좋은 후보를 지워서 로딩을 줄이는 거래** |

---

## 2. v4.0의 선택 — 넓이를 버리고 커버리지를 산다

### 2-1. 워치리스트 (문제 1)

등록 채널이라는 개념을 둘로 쪼갰다.

```
후보 풀 (Pool)          보관만. 스캔 안 함. 상한 8,000
   ↓ 재선별 / 채굴 / 연결 확장
승격 큐 (Queue)         자동 채점 + 재편집 자동 차단. 수동 승인 대기
   ↓ 승인
워치리스트 (Watchlist)  매 실행 100% 전수 스캔. 기본 상한 300
```

v3의 3,353채널은 **후보 풀로 승계**되고 워치리스트에는 자동 등록되지 않는다.

**쿼터 실측(설계값)**

| 작업 | 비용 |
|---|---|
| 워치리스트 300채널 전수 스캔 (깊이 50) | 약 600유닛 = 하루 쿼터의 6% |
| 채널 정보 갱신 300채널 | 약 6유닛 |
| 재선별 1단계 3,000채널 | 약 60유닛 |
| 재선별 2단계 500채널 | 약 1,000유닛 |
| 채굴 키워드 20개 | 검색 20회 |
| 연결 확장 150채널 | 약 150유닛 |

하루에 여러 번 돌려도 남는다. **매 실행이 같은 모집단을 보므로 어제 결과와 오늘 결과를 비교할 수 있다** — 이게 규모를 포기해서 얻은 것이다.

쿼터가 모자라면 "가장 오래 안 본 N채널만"을 제안하되, **전수 커버리지가 깨져 순위 비교가 흔들린다는 점을 확인창에 명시**한다.

### 2-2. 채널 단위 재편집 감지 (문제 2)

영상이 아니라 **채널**을 본다. 승격 큐에서 걸러지므로 워치리스트에 애초에 못 들어온다.

| 신호 | 가중 | 근거 |
|---|---:|---|
| 주 35편 이상 업로드 | +35 | 사람이 기획해 만드는 속도가 아니다 |
| 주 14편 이상 | +25 | |
| 샘플의 85% 이상이 3분 이하 | +28 | 롱폼 벤치마크 대상이 아니다 |
| 샘플의 65% 이상이 3분 이하 | +17 | |
| 중앙 길이 75초 이하 | +10 | |
| 제목 반복도 45% 이상 | +22 | 같은 틀을 계속 찍어낸다 |
| 제목 반복도 30% 이상 | +13 | |
| 제목의 50%에 모음·하이라이트류 단어 | +25 | `모음\|하이라이트\|레전드\|몰아보기\|짤\|다시보기\|풀버전\|편집본\|명장면\|클립\|EP.N\|N회` 등 |
| 제목의 25% 이상 | +14 | |
| 채널명에 짤·클립·모음류 | +15 | |
| 방송사·플랫폼 공식 계열 | +15 | SBS·KBS·MBC·JTBC·tvN·넷플릭스 등 |

**60점 이상이면 자동 차단** (⚙에서 조정). 판정은 확정이 아니라 추정이므로 「그래도 승격」 버튼을 남겨 뒀고, 채널 근거 화면에 **걸린 신호를 사람 말로** 전부 보여준다 — "주 38편 — 사람이 기획해 만드는 속도가 아님" 식으로.

제목 반복도는 두 가지 중 큰 값을 쓴다: ① 가장 흔한 토큰의 등장 비율 ② 제목 쌍의 평균 Jaccard.

**워치 적합도(fit)** — 재편집 아님 32 · 구독자 구간 20 · 롱폼 비율 18 · 활동성 10 · 평소 성과 12 · 아웃라이어 배출 이력 8. 구독자 구간은 1천~10만이 만점이고 100만은 0.4, 500만은 0.05로 급감한다.

### 2-3. 백분위 랭킹 (문제 3)

절대 상한 정규화를 버리고 **후보 풀 내 백분위**로 바꿨다.

| 축 | 가중 | 값 |
|---|---:|---|
| 조회 규모 | 30 | `views` |
| 구독 대비 | 25 | `views / subs` |
| 채널 대비 | 30 | 나이 보정 채널배수 |
| 시간당 속도 | 15 | 최근 VPH (없으면 Lifetime VPH) |

동점은 평균 순위. 백분위는 정의상 균등분포라 **포화가 불가능하다** — 합성 100건 테스트에서 고유 점수 97개, 만점 1건.

**대가와 그 처리**
- 백분위는 상대적이다. 후보 전체가 부실하면 그중 1등도 높게 나온다 → 화면 어디서나 **실제 수치를 점수 옆에 함께** 표시하고, 근거 화면에 "로드된 N건 안의 상대 순위"라고 명시한다. 절대 게이트(조회수·구독자 하한)는 필터로 따로 남긴다.
- **축 결손 처리** — v3.0은 "누락 항목은 0점, 재분배 없음"을 원칙으로 삼았고 그래서 baseline 표본이 부족한 신생 채널이 영구히 하위로 밀렸다(v3.1에서 `isGem` 예외로 땜빵). v4는 **남은 축으로 재정규화하되 신뢰도를 분리해 표시**한다: `근거 3/4축`. 정렬 2차 기준이 conf라 같은 점수면 축이 완비된 쪽이 앞선다.

### 2-4. 부분 로드 (문제 4)

`videos` 스토어에 `by_disc`(discoveredAt) · `by_ch`(channelId) 인덱스를 추가하고 (IndexedDB v2 업그레이드, **기존 데이터는 그대로**), 시작 시 역방향 커서로 **최근 400건만** 읽는다. 5.8만 건이 쌓여 있어도 400건만 파싱한다.

- 더 필요하면 「이전 스캔 결과 더 불러오기」로 커서를 이어 받는다 (백분위는 그때 재계산)
- 보관 정리는 화면이 뜬 뒤 `requestIdleCallback`에서 돈다
- 정리 규칙은 30일 경과 + **채널당 상위 12건** — 좋은 후보를 지워 로딩을 줄이는 거래가 아니다

---

## 3. 그 밖의 변경

- **탭 4개** — ① 워치리스트 ② 후보 발굴 ③ 아웃라이어 ④ 벤치마크
- **영상별 근거 화면 신설** — 점수 4축 분해(백분위 + 실제 수치) · 채널 성과 분포 SVG(같은 형태 이전 영상을 날짜×조회수 로그로 찍고 이 영상을 강조, 중앙값 선) · 기준선을 만든 영상 목록 · 조회수 추이 스냅샷 · 채널 프로파일 · 판정 근거와 주의사항
- **채널 근거 화면 신설** — 적합도 6개 구성요소, 재편집 판정 신호 목록, 원시 수치
- **다크 모드** 정식 지원 (`data-t` 속성 + 토큰 재정의)
- **재측정** — 조회수만이 아니라 VPS·채널배수·백분위까지 함께 갱신. 최소 간격(기본 20분) 전에는 스냅샷을 추가하지 않는다
- **연결 확장** — 워치리스트 상위 채널만 타고 간다. 한글 채널명 필터 기본 유지 (v2.5의 CNN·GMA 유입 차단 장치)
- **전체 백업/복원** — 워치리스트·후보 풀·큐·차단 목록·설정을 JSON 한 파일로

**승계되는 것**: 벤치마크 저장분(IndexedDB `benchmarks`, 손대지 않음) · 스냅샷 · baseline 캐시 · v3의 채널 유니버스(후보 풀로) · API 키.
**승계되지 않는 것**: 워치리스트는 빈 상태로 시작한다. ② 재선별로 다시 뽑아야 한다.

---

## 4. 첫 실행 순서

1. API 키 저장
2. ② 후보 발굴 → **A 기존 채널 재선별** (1단계 약 60유닛 → 2단계 상위 500채널 약 1,000유닛)
3. 승격 큐에서 **적합도 60 이상 일괄 승인** — 재편집 의심은 이미 자동 차단돼 있다
4. ① 워치리스트 → **전수 스캔** (최근 7일)
5. ③ 아웃라이어 → 🎬 롱폼 대박 → 카드를 눌러 **근거** 확인 → ♡ 저장
6. 20분 뒤 ⏱ 재측정 한 번 더 (가속도는 두 시점의 차이라 최소 두 번 필요)

v3.2의 운영 조정 「짤·예능 재편집 채널 정리 필요」는 2단계에서 자동으로 처리된다.

---

## 5. 검증 결과와 범위

**JavaScript 문법 검사 통과. 회귀 테스트 63개 중 63개 통과. 브라우저 렌더 검증 22개 전부 통과 · 콘솔 에러 0건.**

회귀 스위트(합성 입력 · Node vm + DOM 스텁):

| 그룹 | 검증 내용 |
|---|---|
| 1 | 구 방식의 100점 포화 재현 → 백분위에서 고유 점수 97/100, 만점 1건, 동점 평균 순위 |
| 2 | 축 결손 재정규화 · conf 계산 · 전 축 null일 때 0으로 나누지 않음 · 신생 채널이 하위로 밀리지 않음 |
| 3 | 재편집 감지 — 예능 재편집/쇼츠 양산은 차단, 정상 롱폼은 통과, 방송사 가점, 제목 반복도 |
| 4 | 적합도 — 구독자 밴드, 비공개 중립 처리, 휴면 채널 스캔 제외 |
| 5 | 나이 보정 `f(t)=t/(t+τ)` · **v3.1 검증값 재현(원배수 ×1.33 → 보정 ×5.33)** · 자기 제외 · 미래 영상 제외 · 성숙 표본 우선 · 쇼츠/롱폼 분리 · 표본 부족 시 판정 보류 · 작은 기준선 하한 · 라이브 제외 |
| 6 | 형태 판정(3분 이하는 '후보'로만) · ISO duration · 중앙값 · 이스케이프 · 조사 제거 |
| 7 | 반복 소재 — 3채널 이상 군집, 내용어 1개 공유는 군집 아님 |
| 8 | **채널 상한이 정렬 다음에 적용됨** · 제외 사유 기록 · 0건과 가려짐 구분 · 구독 비공개를 가짜값으로 채우지 않음 · 한글 필터가 프리셋으로 풀리지 않음(v3.2 #1 재발 방지) |
| 9 | 쿼터 — **미국 PT 자정 초기화**(KST 아님) · 한도 초과 시 요청 전 차단 |
| 10 | v3 승계 — 유니버스→후보 풀, 워치리스트 자동 등록 안 함, 승계 1회, 상한 초과 승격 방지, 차단 채널 재유입 방지, 내린 채널 보존 |
| 11 | 스냅샷 — 1개면 VPH 없음, 2개면 Lifetime 대비, 3개면 직전 구간 대비, 0 증가 시 폴백 |
| 12 | 부분 로드 — IndexedDB 없어도 예외 없음, 요청 개수 준수, before 커서 페이지네이션 |

브라우저 검증(Playwright · Chromium): 렌더·탭·빈 상태·합성 100건 랭킹+렌더 23ms·갤러리·근거 모달 4축·채널 근거·제외 사유·표 뷰·다크 모드·390px 가로 스크롤 0px.

**수행하지 않은 것**: 실제 API 키를 사용한 검색, 실시간 영상 수치, 실제 브라우저에서의 클릭 조작 검증, GitHub Pages 배포. 재편집 감지의 오탐·미탐률은 실제 채널로 측정하지 않았다 — 합성 입력에서 의도한 대로 동작하는 것까지만 확인했다.

---

## 6. 알려진 한계

- 워치리스트에 없는 채널의 영상은 **애초에 후보가 아니다**. 넓이를 포기하고 깊이를 택한 설계다. 새 채널은 ② 채굴로만 들어온다.
- `search.list`는 유튜브 전체를 반환하지 않는다. 채굴의 누락은 구조적이다.
- 백분위는 **상대적**이다. 로드된 후보가 바뀌면 점수가 바뀐다. 절대 성과를 보려면 실제 수치를 봐야 한다.
- 재편집 채널 판정은 업로드 샘플 기반 **추정**이다. 오탐 가능 — 특히 롱폼과 쇼츠를 병행하는 채널.
- 나이 보정 τ(쇼츠 24h / 롱폼 72h)는 일반 곡선의 근사값이다. 스냅샷이 2~3주 쌓이면 채널별 실측 τ로 교체할 수 있다.
- Shorts는 3분 이하 + 화면비 요건인데 Data API가 화면비를 주지 않아 **확정 판정 불가** → '≤3분 후보'로만 표기.
- 반복 소재는 제목 텍스트만 본다. 표현이 다르고 내용이 같으면 놓친다.
- 썸네일 이미지는 AI에 전송하지 않는다. AI의 썸네일 언급은 제목 기반 가설이다.
- 구독자를 숨긴 채널은 조회/구독 축이 빠진다 (가짜값을 만들지 않는다).
- **브라우저를 닫으면 어떤 추적도 되지 않는다.** 백그라운드 수집기가 아니다.
- 쿼터 카운터는 요청 시점의 보수적 추정이다. 실제 사용량은 Cloud Console에서 확인해야 한다.

---

## 7. 다음에 볼 것

- **재편집 감지 정확도 측정** — 재선별 2단계 결과에서 차단된 채널 30개를 직접 열어 보고 오탐률을 기록한다. 오탐이 20%를 넘으면 차단 기준을 70으로 올린다.
- **백분위 안정성** — 같은 영상의 점수가 로드 건수에 따라 얼마나 움직이는지 기록한다. 흔들림이 크면 풀을 "최근 30일 고정"으로 묶는 방안이 있다.
- **워치리스트 적정 크기** — 300에서 시작해 「30일 이상 아웃라이어 0건」 정리를 두 번 돌린 뒤 남는 수가 실질 크기다.
- **채널별 실측 τ** — 스냅샷 2~3주 축적 후.
- 평가 지표는 v3.0에서 정한 것을 그대로 쓴다: **상위 20개 중 실제 저장할 영상 수**, **새로 찾은 소형 채널 수**, **검색 1회당 새로운 유효 후보 수**.
